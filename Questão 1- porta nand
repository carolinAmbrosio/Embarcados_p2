library IEEE;
use IEEE.std_logic_1164.all;

entity Nand is
	 port(
		 a, b : in bit;
		 x : buffer bit;
		 saida : out bit
	     );
end Nand;

architecture Nand of Nand is
begin

	x <=NOT (a AND b);
	
	saida <= NOT x;

end Nand;
