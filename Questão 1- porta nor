library IEEE;
use IEEE.std_logic_1164.all;

entity Nor is
	 port(
		 a, b : in bit;
		 x : buffer bit;
		 saida : out bit
	     );
end Nor;

architecture Nor of Nor is
begin
	
	x <= NOT(a OR b);
	
	saida <= NOT x;

end Nor;
